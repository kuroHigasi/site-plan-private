# 画面遷移図

ポートフォリオサイトの画面遷移図叩き台です（Mermaid 記法）。

---

## 画面遷移図

```mermaid
flowchart TD
    TOP["🏠 トップページ (/)"]
    ABOUT["👤 プロフィール (/about)"]
    ABOUT_FE["💻 フロントエンド (/about/frontend)"]
    ABOUT_BE["⚙️ バックエンド (/about/backend)"]
    ABOUT_DESIGN["🎨 デザイン (/about/design)"]
    ABOUT_OTHER["📦 その他 (/about/other)"]
    WORKS["🗂️ 作品一覧 (/works)"]
    GAMES["🎮 ゲーム一覧 (/works/games)"]
    GAME_DETAIL["🎮 ゲーム詳細 (/works/games/:id)"]
    PORTFOLIO["🌐 本サイト (/works/portfolio)"]
    UI["🖼️ UI一覧 (/works/ui)"]
    UI_DETAIL["🖼️ UI詳細 (/works/ui/:id)"]
    CERT["📋 資格・学習コンテンツ (/certifications)"]
    CERT_LIST["🏆 取得資格一覧 (/certifications/list)"]
    CERT_CURRENT["📖 現在学習中 (/certifications/current)"]
    CERT_CONTENTS["📚 学習コンテンツ一覧 (/certifications/contents)"]
    CERT_DETAIL["📄 学習コンテンツ詳細 (/certifications/contents/:id)"]
    CONTACT["✉️ お問い合わせ (/contact)"]

    TOP --> ABOUT
    TOP --> WORKS
    TOP --> CERT
    TOP --> CONTACT

    ABOUT --> ABOUT_FE
    ABOUT --> ABOUT_BE
    ABOUT --> ABOUT_DESIGN
    ABOUT --> ABOUT_OTHER

    WORKS --> GAMES
    WORKS --> PORTFOLIO
    WORKS --> UI

    GAMES --> GAME_DETAIL
    UI --> UI_DETAIL

    CERT --> CERT_LIST
    CERT --> CERT_CURRENT
    CERT --> CERT_CONTENTS
    CERT_CONTENTS --> CERT_DETAIL
```

---

## グローバルナビゲーション

グローバルナビゲーションから任意のページへ直接遷移可能です。

```mermaid
flowchart LR
    NAV["🔗 グローバルナビ"]
    TOP["トップページ (/)"]
    ABOUT["プロフィール (/about)"]
    WORKS["作品一覧 (/works)"]
    CERT["資格・学習コンテンツ (/certifications)"]
    CONTACT["お問い合わせ (/contact)"]

    NAV --> TOP
    NAV --> ABOUT
    NAV --> WORKS
    NAV --> CERT
    NAV --> CONTACT
```

---

## フッター導線

全ページのフッターから遷移可能なリンクです。ログインはグローバルナビには掲載しません。

```mermaid
flowchart LR
    FOOTER["📎 フッター"]
    PRIVACY["プライバシーポリシー (/privacy)"]
    CHANGELOG["変更履歴 (/changelog)"]
    LOGIN["ログイン (/login)"]
    ADMIN["管理画面（URL 未定義）"]

    FOOTER --> PRIVACY
    FOOTER --> CHANGELOG
    FOOTER --> LOGIN
    LOGIN -->|"ログイン成功"| ADMIN
```

---

## 備考・検討事項

- トップページには各セクションへのバナー・カードリンクを設ける
- 詳細ページには「一覧へ戻る」リンクを設ける。プロフィール詳細（P002-01〜04）には「プロフィールへ戻る」リンクを設ける
- 全ページにグローバルナビゲーションとフッターを配置する
- `/privacy`、`/changelog`、`/login` はグローバルナビ・メイン遷移図には含めず、フッター導線セクションで定義する
- プロフィール（P002）のスキルセットカードから各スキル詳細ページ（P002-01〜04）へ遷移する。準備中のカードはリンクを非表示とする
- ログイン（P008）はフッターからのみ遷移する。`/login` は検索エンジン向け sitemap からの除外・`noindex` を推奨
- ログイン成功後の管理画面 URL は別リポジトリで定義する（`01_DOCS/05_セキュリティ管理/00_概要.md` 参照）。定義後にフッター導線図を更新する
